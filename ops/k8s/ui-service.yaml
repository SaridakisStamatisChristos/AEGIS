apiVersion: v1
kind: Service
metadata:
  name: aegisrun-ui
  namespace: aegisrun
  labels:
    app.kubernetes.io/name: aegisrun
    app.kubernetes.io/component: ui
spec:
  type: ClusterIP
  ports:
    - name: http
      port: 80
      targetPort: http
      protocol: TCP
  selector:
    app.kubernetes.io/name: aegisrun
    app.kubernetes.io/component: ui
